{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ classe.code }}</td>
            </tr>
            <tr>
                <th>Salle</th>
                <td>{{ classe.salle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_classe_index') }}">back to list</a>

    <a href="{{ path('admin_classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('admin/classe/_delete_form.html.twig') }}
{% endblock %}
