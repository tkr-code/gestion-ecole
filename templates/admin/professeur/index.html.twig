{% extends 'admin/base.html.twig' %}
{% import "macros/dataTable.html.twig" as dataTable %}
{% import "macros/links.html.twig" as links %}
{% import "macros/breadcrumb.html.twig" as breadcrumb %}
{% import "macros/alertes.html.twig" as alertes %}

{% block title %}Professeurs{% endblock %}

{% block breadcrumb %}
    {{ breadcrumb.breadcrumb('Professeurs') }}
{% endblock %}

{% block head %}
    {{ dataTable.css() }}
{% endblock %}

{% block foot %}
    {{ dataTable.js() }}
{% endblock %}

{% block javascripts %}
    {{ dataTable.script('#table-1') }}
{% endblock %}

{% block body %}
{% for item in app.flashes('success') %}
    {{ alertes.alert(item,'success')}}
{% endfor %}
<div class="card card-outline card-primary">
    <div class="card-header">
        <h3>Professeurs</h3>
    </div>
    <div class="card-body">
        <div class="btn-group">
            {{ links.new(path('admin_professeur_new')) }}
        </div>
        <table class="table" id="table-1">
            <thead>
                <tr>
                    <th>Professeur</th>                    
                    <th>Matières</th>                    
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
                {% for user in professeurs %}
                <tr>
                    <td>{{ user.personne.fullName }} - {{ user.professeur.titre }}</td>
                    <td>
                        {% for item in user.professeur.matieres %}
                            {{ item.designation }}
                        {% endfor %}
                    </td>
                    <td>
                        <div class="btn-group">
                            {{ links.show(path('admin_professeur_show', {'id': user.id}))}}
                            {{ links.edit(path('admin_professeur_edit', {'id': user.id}))}}
                        </div>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div> 
{% endblock %}