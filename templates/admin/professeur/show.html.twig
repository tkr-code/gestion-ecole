{% extends 'base.html.twig' %}

{% block title %}Professeur{% endblock %}

{% block body %}
    <h1>Professeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professeur.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ professeur.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_professeur_index') }}">back to list</a>

    <a href="{{ path('admin_professeur_edit', {'id': professeur.id}) }}">edit</a>

    {{ include('admin/professeur/_delete_form.html.twig') }}
{% endblock %}
