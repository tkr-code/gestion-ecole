{% extends 'admin/base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ etudiant.matricule }}</td>
            </tr>
            <tr>
                <th>Occupation</th>
                <td>{{ etudiant.occupation }}</td>
            </tr>
            <tr>
                <th>Signature</th>
                <td>{{ etudiant.signature }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_etudiant_index') }}">back to list</a>

    <a href="{{ path('admin_etudiant_edit', {'id': etudiant.id}) }}">edit</a>

    {{ include('admin/etudiant/_delete_form.html.twig') }}
{% endblock %}
