{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="btn-group">
        <a class="btn btn-dark" href="{{ path('admin_etudiant_index') }}">back to list</a>
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
