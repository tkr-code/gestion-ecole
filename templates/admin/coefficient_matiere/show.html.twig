{% extends 'base.html.twig' %}

{% block title %}CoefficientMatiere{% endblock %}

{% block body %}
    <h1>CoefficientMatiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coefficient_matiere.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ coefficient_matiere.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_coefficient_matiere_index') }}">back to list</a>

    <a href="{{ path('admin_coefficient_matiere_edit', {'id': coefficient_matiere.id}) }}">edit</a>

    {{ include('admin/coefficient_matiere/_delete_form.html.twig') }}
{% endblock %}
